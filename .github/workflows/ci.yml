name: CI
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: typst-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  ci:
    uses: edithatogo/repository-standards/.github/workflows/typst-ci.yml@ad67bedaa0c4d0769bd54fd76354bac65b25b88c # main
